doctype html
html
  head
    title Snallabot Dashboard
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossOrigin="anonymous")
  body
    div(style={padding: "16px 8px"})
      h2 Which EA account should we use?
      form(action="/dashboard/selectLeague" method="POST")
        .input-group.mb-3
          span.input-group-text EA Access Token
          input#access(type="text" name="access_token" class="form-control" value=access_token readonly=true aria-label="EA access token")
        if discord
          .input-group.mb-3
            span.input-group-text Discord League
            input#discord(type="text" name="discord" class="form-control" value=discord readonly=true aria-label="Discord League (debug)")
        select#selectedPersona(class="form-select" name="selected_persona")
          each persona in personas
            option(value=`${JSON.stringify(persona)}`) #{persona.displayName} - #{namespaces[persona.namespaceName]} - #{persona.maddenEntitlement}
        button#submit(type="submit" class="btn btn-primary") Submit EA Account
